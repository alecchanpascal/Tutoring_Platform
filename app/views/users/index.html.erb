<% @users.each do |user| %>
    <div class="card">
        <div class="card-body">
            <h4 class="card-title"><%= link_to user.username, user_path(user) %></h4>
        </div>
    </div>
<% end %>